 </nav>
<p id="notice"><%= notice %></p>

<h1>Listing Pins</h1>

  <div class="container">
    <div class="row">
      <div class="col s12 m4">
        <div class="card">
          <!--유저 개인 정보 -->
          <div class="card user-info">
            <div class="card-image">
              <img src="<%= current_user.avatar.url %>" />
            </div>
            
          <div class="card-content">
            <div class="card-title"><%= current_user.username %></div>
              <p>국적 :  <div class="country-select">
                  <div class="flag <%= current_user.country_code %>">          
                  <p><%= current_user.country%></p>
                  </div>
                </div>
              <p>이메일 : <%= current_user.email %></p>
              <p>팔로워 : <%= current_user.followers.length %></p>
              <p>팔로잉 : <%= current_user.followings.length %></p>
              <p>올린 글 개수 : <%= @pins_count %></p>
                <%= link_to new_pin_path do %>
                  <button class="btn">글 쓰기</button>
                <% end %>
          </div>
        </div>
       </div>  
      </div>
    
    
    
 
 
    
    
    
      <div class=" col s12 m8">
        
        <!--작성글(sns) 리스트 -->
        <% @pins.each do |pin| %>
          <% if current_user.id == pin.user.id %>
            <%= render partial: "pin", object: pin %>        
          <% end %>
        <% end %>
      </div>
    </div>
  </div>

 
 
 
          <!--test용 -->
    <div class="container" >
      <!--작성글(레시피) 리스트 -->
      <% @recipe_blogs.each do |recipe_blog| %>
         <%= recipe_blog.title %>
         <%= recipe_blog.r_description %>
      <%end%>    
      
      
    </div>
 
 
 
 
 <!--test 용-->
 


<table>
  <thead>
    <tr>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pins.each do |pin| %>
      <tr>
        <td><%= pin.p_description %></td>
        <td><%= link_to 'Show', pin %></td>
        <td><%= link_to 'Edit', edit_pin_path(pin) %></td>
        <td><%= link_to 'Destroy', pin, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pin', new_pin_path %>
